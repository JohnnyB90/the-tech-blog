<div class="container">
  <div class="row justify-content-center mt-5">
    {{#each blogPosts as |post|}}
      <div class="col-lg-12">
          <div class="card-body">
            {{> blogpost.card post}}     
        </div>
      </div>
    {{/each}}
  </div>
</div>
