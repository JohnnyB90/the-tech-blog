<div class="container">
  <div class="row justify-content-center mt-5 text-center">
    <h2>All Blogs</h2>
    {{#each blogPosts as |post|}}
      <div class="col-lg-12">
        <div class="card-body">
          {{> blogpost.card post}}     
        </div>
      </div>
    {{/each}}
  </div>
</div>
